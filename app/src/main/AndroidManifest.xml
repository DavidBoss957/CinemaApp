<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CinemaApp"
        tools:targetApi="31">

        <activity android:name=".ui.SingupActivity"
        android:exported="false"
        android:label="title_activity_signup"
        android:theme="@style/Theme.CinemaApp" />

        <activity android:name=".ui.MovieDetailActivity"
            android:exported="false"
            android:label="movie_detail_activity"
            android:theme="@style/Theme.CinemaApp" />

        <activity android:name=".ui.MoviesActivity"
            android:exported="false"
            android:label="title_activity_movies"
            android:theme="@style/Theme.CinemaApp" />

        <activity android:name=".ui.MoviesAdapter"
            android:exported="false"
            android:label="Adapter"
            android:theme="@style/Theme.CinemaApp"
            />

        <activity
            android:name=".ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>